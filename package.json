{
  "name": "node_app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "node ./esbuild.js",
    "build:exec": "node ./esbuild.js && node dist/index.js",
    "watch": "node ./esbuild.watch.js",
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint 'src/**/*.{js,jsx,ts,tsx}' --fix"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx}": [
      "eslint 'src/**/*.{js,jsx,ts,tsx}' --fix"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
